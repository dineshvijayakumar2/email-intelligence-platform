# Status Update — Week of April 15-21, 2026

## This week's headline: Threading bug fixed

Found and fixed a fundamental bug in canonical thread resolution — same conversation chains were sometimes being split across multiple threads under different contacts. Verified on the Q712657 case (Millfield Rise quote) and rebuilt all 255K emails. Threading is now reliably chained across replies and across mailboxes within a client.

---

## What now works automatically

What was previously manual or unreliable now runs without intervention:

- **Email pipeline runs end-to-end automatically** after each sync — extraction, classification, embedding, AI-based QB reference linking. No admin clicks required.
- **All three sync types** (Gmail, Outlook, QuickBase) run on automated schedules with proper error recovery (auth expiry, token refresh, etc.).
- **74 AI-extracted quote/job links** created automatically in the last 24 hours. The platform now reads email content, identifies quote and job references, validates them against QuickBase, and links the thread to the correct journey.

## Demo: thread journey panel

When AMs open a thread tied to a quote, they now see the journey context inline — quote details, contact, value, all surfaced from QuickBase automatically based on what the AI extracted from the email content.

[Q712657 thread shows: Quote Q712657 — Imogen Butcher — $3,272]

---

## What's ready but not yet visible

- **Contact intelligence data layer is live.** Every contact has a persona classification (champion / active / prospect / inactive / dormant), quote metrics, email engagement scores, and industry benchmarks.
- **20,116 contacts classified** across the persona categories.
- Data is queryable via API but doesn't have a frontend yet.

## Next week: Contact Persona UI

The frontend that surfaces this contact intelligence to AMs. They'll see persona classification, quote behavior, engagement patterns, and how each contact compares to industry benchmarks. Targeting end of week.

---

## Open question for you

After the persona page lands, what would help you most? Options I can build:

1. **AM team performance dashboard** (the conversion analysis we did last week, in the platform)
2. **Operations control center** (one place for admin to see system health, sync status, pipeline health)
3. **Something else you've been thinking about**

---

## Operational note

Upgraded Supabase database tier from micro to small this week — gives more headroom for larger reembed and pipeline operations. Modest cost increase, want to flag before it shows on the bill.

---

## Behind the scenes — appendix

~75 commits across the week. Summary by category:

### Worker infrastructure (W1–W5)
- Background job system: full worker architecture (schema, factory, runner, stuck-job reconciler)
- All `BackgroundTasks` callsites migrated (`ai_backfill`, `ai_analysis`, `email_pipeline`)
- Event-driven notification dispatch with WebSocket rooms and bell UI
- Worker health endpoint, Railway config, dev runner
- Pipeline hardening: auto-resume interrupted pipelines on worker startup, zombie guard, embedding timeout fixes

### Email sync reliability  
- Unified cron architecture: replaced internal async sync loops with external cron-triggered endpoints (Gmail, Outlook, QB)
- Per-mailbox configurable sync intervals, relative timestamp display
- Outlook fixes: auto-recover from 410 delta token expiry, skip system messages
- Batch size tuning (100 → 25 → 10) to avoid Supabase statement timeouts
- Sync errors shown on mailbox list, auto-cleared on new sync

### AI pipeline improvements
- Per-tenant AI settings (cache replaces global singleton, per-client model config)
- AI pre-filter for zero-false-positive hard skips
- Prompt versioning with auto-hash for change detection
- AI-powered QB quote/job extraction (T1.3) with cron dispatch
- Pipeline auto-trigger after date-range email fetch
- Fail-fast on AI errors (no silent model/provider fallback)

### Contact intelligence (C1.1) — new
- 5 SQL views: `contact_persona`, `contact_email_metrics` (materialized), `contact_quote_metrics`, `company_contact_summary`, `industry_benchmarks`
- 5 API endpoints: contact persona, company summary, paginated company contacts, industry benchmarks, industry list
- Materialized view refresh endpoint for cron/post-sync

### Performance and reliability fixes
- Classification health RPC: rewrote LATERAL → GROUP BY (eliminated timeout)
- Company count refresh: bulk CTE replaces per-company loop (migration 091)
- Email classification: eliminated O(N²) per-batch scans
- Strike rate: batched QB quote fetch (was timing out for 10K+ quote customers)
- Embedding pipeline: reduced DB chunk size, SECURITY DEFINER + search_path on RPCs
- Thread splitting: pre-load resolver indexes, sibling lookup fix, re-resolve UI

### UI/UX
- Threads table: per-column text wrapping fixed
- Email drilldown: latest email shown first when opening from threads/contacts
- Journey panel wired into thread view (showing AI-linked quotes)
- Data Health dashboard: filtered count, live polling, redefined coverage metrics
- AU formatting: consistent locale formatting across frontend

### Data integrity
- QB contact linking (migration 081): industry rollup from QB customers
- Junction link dedup: prevents PostgreSQL error 21000 on duplicate batches
- Extraction jobs constraint: added 'interrupted' status to check constraint
- Incremental extraction: sync-triggered pipelines use incremental mode, not full re-extract

**Totals:** 75 commits, ~10 migrations, full worker migration complete, contact intelligence data layer shipped, cron architecture unified, multiple production timeout/reliability fixes.